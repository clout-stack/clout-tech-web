/**
 * Subscriber Model
 */

const Subscriber = module.exports = {
	/**
	 * Create a new subscriber
	 * @param  {[type]}   email subscriber email
	 * @return {Promise}        promise
	 */
	create() {
		return Promise.resolve();
	}
};
